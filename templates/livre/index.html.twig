{% extends 'base.html.twig' %}

{% block title %}Bienvenue dans mon index de livres !{% endblock %}

{% block body %}
<h1>Bienvenue dans mon index de livres</h1>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Titre</th>
            <th> &nbsp;</th>
        </tr>
    </thead>
    <tbody>
  {% for livre in livres %}
      

        <tr>
            <td>{{ livre.Id }}</td>
            <td>{{ livre.titre }}</td>
            <td><a href="{{ path('livre_show', {'id': livre.id}) }}">consulter</a></td>
            

        </tr>
    
  {% endfor %} 
  </tbody>
  </table>

  <a href="{{ path('home') }}">Retour</a>

{% endblock %}
