{% extends 'base.html.twig' %}

{% block body %}
	<h1>
		Jolibrairie n°
		{{ librairie.id }}
	</h1>

	{% dump librairie %}

	<table class="table">
		<tbody>
			<tr>
				<th>Description</th>
				<td>{{ librairie.description }}</td>
			</tr>
			<tr>
				<th>
					Amateur
				</th>
				<td>
					<a href="{{ path('amateur_show', {'id': librairie.amateur.id}) }}">{{librairie.amateur.nom}}</a>
				</td>
			</tr>
			<tr>
				<table class="table">
					<thead></thead>
					<tr>
						<th>Livres</th>
						<td>
							<a href="{{ path('livre_new', {'id': librairie.id}) }}">Ajouter un nouveau livre</a>
						</td>
					</tr>
				</thead>
			</tr>
		</tbody>
		<tbody>
			<tr>
				<th>
					Titre
				</th>
			</tr>
			{% for livre in librairie.livres %}

				<tr>
					<td>


						<a href="{{ path('livre_show', {'id' : livre.id}) }}">{{ livre }}</a>


					</td>
				</tr>

			{% endfor %}
		</tbody>
	</table>
</tr></tbody></table><button class="w3-btn"><a href="javascript:history.go(-1)">Retour</a></button>{% endblock %}
