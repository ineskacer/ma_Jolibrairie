<form method="post" action="{{ path('app_librairie_delete', {'id': librairie.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette librairie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ librairie.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
