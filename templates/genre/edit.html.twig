{% extends 'base.html.twig' %}

{% block title %}Modification de l'étalage
{% endblock %}

{% block body %}
	<h1>Modification de l'étalage</h1>

	{{ include('genre/_form.html.twig', {'button_label': 'Valider'}) }}
	{{ include('genre/_delete_form.html.twig') }}
		<button class="w3-btn">
			<a href="{{ path('livres_list') }}">Modifier le contenu</a>
		</button>
		<button class="w3-btn">
			<a href="{{ path('genres_list') }}">Retour</a>
		</button>
	{% endblock %}
