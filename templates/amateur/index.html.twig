{% extends 'base.html.twig' %}

{% block title %}Bienvenue!{% endblock %}

{% block body %}
<h1>Index des amateurs</h1>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th> &nbsp;</th>
        </tr>
    </thead>
  {% for amateur in amateurs %}
      <tbody>

        <tr>
            <td>{{ amateur.Id }}</td>
            <td>{{ amateur.nom }}</td>
            <td><td><a href="{{ path('amateur_show', {'id': amateur.id}) }}">Découvre en plus !</a></td></td>
        </tr>
    </tbody>
  {% endfor %} 
  </table>
  <a href="{{ path('home') }}">Retour</a>

{% endblock %}
