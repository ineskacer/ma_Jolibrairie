{% extends 'base.html.twig' %}

{% block body %}
	<h1>
		{{ amateur.nom }}
	</h1>

	{% dump amateur %}

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ amateur.id }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ amateur.description }}</td>
			</tr>
			<tr>
				<table class="table">
					<thead></thead>
					<tr>
						<th>Jolibraires</th>
						<td>
							<a href="{{ path('librairie_new', {'id': amateur.id}) }}">Ajouter une nouvelle Jolibrairie</a>
						</td>
					</tr>
				</thead>
			</tr>
		</tbody>
		<tbody>
			<tr>
				<th>
					Description
				</th>
			</tr>
			{% for librairie in amateur.librairie %}
				<tr>
					<td>
						<a href="{{ path('librairie_show', {'id': librairie.id}) }}">{{ librairie }}</a>
					</td>
				</tr>
			{% endfor %}

		</tbody>
	</tbody>
</table></tbody></table></tr></tbody></table><button class="w3-btn"><a href="{{ path('amateurs_list') }}">Retour</a></button>{% endblock %}
