{% extends 'base.html.twig' %}

{% block body %}
<h1>
	{{ amateur.nom }}
</h1>

{% dump amateur %}

<table class="table">
	<tbody>
		<tr>
			<th>Id</th>
			<td>{{ amateur.id }}</td>
		</tr>
		<tr>
			<th>Description</th>
			<td>{{ amateur.description }}</td>
		</tr>
		<tr>
			<table class="table">
				<thead></thead>
				<tr>
					<th>Jolibraires</th>
				</tr>
			</thead>
		</tr>
	</tbody>
	<tbody>
		<tr>
			<th>
				Titre
			</th>
			<th>
				Action
			</th>
		</tr>
		{% for librairie in amateur.librairie %}
			<tr>
				<td>
					{{librairie.description}}</td>
				<td>
					<a href="{{ path('librairie_show', {'id': librairie.id}) }}">consulter</a>
				</td>
			</tr>
{% endfor %}		</tbody>
	</tbody>
</table></tbody></table></tr></tbody></table><a href="{{ path('amateurs_list') }}">Retour</a>{% endblock %}
